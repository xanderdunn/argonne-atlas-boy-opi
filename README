This is an entire Eclipse workspace for the ATLAS CSS BOY OPI.  It is under git version control such that a user can clone this repository and then point the CSS workspace dialogue to the cloned directory.  This creates a complete CSS BOY set-up with all the desired ATLAS screens and settings.

This repository is intended to be used with the ATLAS project's start_epics_cavCtl or start_css scripts.  These scripts should keep your local repository up-to-date, as well as start CSS BOY with the correct parameters.  These scripts are located in /net/phoebus/spxfshare/scripts/
